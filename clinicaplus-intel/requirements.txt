# ============================================================
# requirements.txt — Verificado e alinhado em 30 de Março 2026
# ============================================================

# Web Framework
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.0

# Database
redis>=7.0.0              # redis-py 7.x (Python >=3.10). Latest: 7.1.1
hiredis>=2.3.2            # parser C opcional — acelera throughput
langchain-redis>=0.2.5    # era 0.1.2 — breaking changes em 0.2.x (Redis Sentinel, ToolMessage fix)
asyncpg>=0.29.0

# LangGraph & Google AI Stack
langchain>=1.2.0           # era 0.3.25 — major bump, nova API de agentes
langchain-core>=1.2.0      # era 0.3.83 — alinhado com langchain>=1.x
langchain-community>=0.4.0 # era 0.3.25 — alinhado com langchain>=1.x
langchain-google-genai>=2.0.0
langchain-groq>=0.2.0
langchain-anthropic>=0.3.0
langgraph>=1.1.0
langgraph-checkpoint>=4.0.0    # era 2.1.2 — formato de armazenamento redesenhado
langgraph-checkpoint-postgres>=2.0.0  # Persistência LangGraph via Postgres
langgraph-checkpoint-redis>=0.4.0  # era 0.2.2 — mantido para compatibilidade (cost tracking)
psycopg[binary,pool]>=3.1.0    # Driver async Postgres requerido pelo checkpoint-postgres
langsmith>=0.7.0           # era 0.3.45

# Google Auth (dependências transitivas — não fixar manualmente)
# google-api-core e google-auth são geridos por langchain-google-genai
# google-ai-generativelanguage idem — remover para evitar conflitos de versão

# Ferramentas Utilitárias
APScheduler>=3.11.0

# Test & Dev
pytest>=7.4.3
pytest-asyncio>=0.23.0    # 0.21.x tem breaking change no modo asyncio
httpx>=0.27.0
